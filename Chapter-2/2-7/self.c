/*
  Write a function invert(x,p,n) that returns x with 
  the n bits that begain at position p inverted (i.e., 
  1 changed into 0 and vice versa), leaving the other 
  unchanged.
*/

#include <stdio.h>

unsigned invert(unsigned int x, int p, int n, unsigned int y)
{
  
}